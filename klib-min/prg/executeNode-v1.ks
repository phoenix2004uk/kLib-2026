{local D is import("sys/steering-v1"):awaitSteering,B is{parameter H,C is stage:number. local B is 0,A is 0,D is 0,F is 0,G is constant:g0. for A in ship:parts if A:stage<=C set B to B+A:mass. for B in ship:engines if(C<stage:number and B:stage=C)or(C=stage:number and B:ignition and not B:flameout)set D to D+B:possiblethrustat(ship:q),A to A+B:ispat(ship:q),F to F+1.if F>0set A to A/F. if D=0or A=0return 0.return G*B*A*(1-constant:e^(-abs(H)/(G*A)))/D.}.export(lex("burnDuration",B,"warpToNode",{parameter A is 60.if not hasnode return. warpTo(time:seconds+nextnode:eta-B(nextnode:deltav:mag/2)-A).},"executeNode",{parameter F is 60.if not hasnode return. local A is nextnode,C is A:deltaV,B is B(C:mag/2).wait until A:eta<=B+F. kuniverse:timewarp:cancelwarp().wait until kuniverse:timewarp:issettled. lock steering to A:deltaV. D().wait until A:eta<=B. lock throttle to max(.001,min(A:deltaV:mag*mass/availableThrust,1)).wait until C*A:deltaV<0or(A:deltaV:mag<.01and C*A:deltaV<.5).lock throttle to 0.unlock steering.wait.1.remove A.})).}