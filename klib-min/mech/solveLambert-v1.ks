{local F is import("util/math-v1"),D is constant:PI,G is constant:DegToRad,M is constant:RadToDeg,B is{parameter A.return lex("valid",false,"reason",A).},H is{parameter A,B,H.local J is abs(A-1),I is B^2.if J<.2and J>.01{local C is 1/(1-A^2).if C>0{local F is 2*arccos(max(-1,min(1,A)))*G,A is 2*arcsin(sqrt(max(0,min(1,I/C))))*G. if B<0set A to-A. return C*sqrt(C)*((F-sin(F*M))-(A-sin(A*M))+2*D*H)/2.}local D is 2*F:acosh(A),A is 2*F:asinh(sqrt(-I/C)).if B<0set A to-A. return-C*sqrt(-C)*((A-F:sinh(A))-(D-F:sinh(D)))/2.}local C is A^2-1,L is abs(C),F is sqrt(max(0,1+I*C)).if J<.01{local F is F-B*A,I is.5*(1-B-A*F),G is 1,C is 1.from{local A is 0.}until abs(C)<=1e-11or A>=1e3step{set A to A+1.}do set C to C*(3+A)*(1+A)/(2.5+A)*I/(A+1),G to G+C. return 2*F^3*G/3+2*F*B+H*D/L^1.5.}local M is sqrt(L),N is A*F-B*C,K is 0.if C<0set K to H*D+arccos(max(-1,min(1,N)))*G. else set K to ln(M*(F-B*A)+N).return(A-B*F-K/M)/C.},K is{parameter A,H,I.local B is I^2,D is B*I,C is 1-A^2,F is sqrt(1-B*C),J is F^3,G is(3*H*A-2+2*D*A/F)/C,K is(3*H+5*A*G+2*(1-B)*D/J)/C. return V(G,K,(7*A*K+8*G-6*(1-B)*B*D*A/J/F^2)/C).},L is{parameter I,C,D,F,L.local A is C. from{local C is 0.}until C>=15step{set C to C+1.}do{local G is H(A,D,F),F is K(A,G,D),D is G-I,H is F:x,I is F:y,J is H^2,K is H*(J-D*I)+F:z*D^2/6.if abs(K)<1e-15return B("Lambert Householder denominator is singular").local B is A-D*(J-D*I/2)/K. if abs(A-B)<L return lex("valid",true,"x",B,"iterations",C+1).set A to B.}return B("Lambert Householder iteration did not converge").},J is{parameter A,F,C,N.local I is F^2,M is I*F,G is arccos(max(-1,min(1,F)))*G+F*sqrt(max(0,1-I)),J is C>0and C>floor(A/D).if C>0and not J{local D is G+C*D. if A<D{local B is 0.from{local A is 0.}until A>=13step{set A to A+1.}do{local A is K(B,D,F),G is A:x,I is A:y,J is I^2-G*A:z/2.if abs(J)<1e-15break. local K is B-G*I/J. if abs(B-K)<1e-13break. set B to K,D to H(B,F,C).}set J to A<D.}}if J return B("Requested Lambert revolution count has no solution").if C=0{local B is 2/3*(1-M),C is 0.if A>=G set C to-(A-G)/(A-G+4).else if A<=B set C to B*(B-A)/((2/5)*(1-I*M)*A)+1.else set C to(A/G)^(ln(2)/ln(B/G))-1.return L(A,C,F,0,1e-5).}local H is 0.if N="left"set H to((C*D+D)/(8*A))^(2/3).else if N="right"set H to(8*A/(C*D))^(2/3).else return B("Lambert multi-revolution branch must be left or right").return L(A,(H-1)/(H+1),F,C,1e-8).}.export({parameter L,M,_,N,G is"short",O is 0,_A is"left",P is false. if _<=0return B("Lambert time of flight must be positive").if N<=0return B("Lambert gravitational parameter must be positive").if G<>"short"and G<>"long"return B("Lambert direction must be short or long").if O<0return B("Lambert revolution count cannot be negative").local C is L:mag,D is M:mag. if C<=0or D<=0return B("Lambert position vectors must be non-zero").local F is(M-L):mag. if F<=1e-9return B("Coincident Lambert endpoints are not supported").local H is(F+C+D)/2,S is L/C,T is M/D,U is max(0,min(1,1-F/H)),A is sqrt(U),_B is-vcrs(S,T),I is false. if _B:mag>1e-10set I to _B:normalized. else{if not P:istype("Vector")return B("Lambert transfer plane is undefined; provide planeNormal").if P:mag<=1e-10return B("Lambert planeNormal must be non-zero").set I to P:normalized.}local W is(-vcrs(I,S)):normalized,X is(-vcrs(I,T)):normalized. if G="long"set A to-A,W to-W,X to-X. local _C is sqrt(2*N/H^3)*_,J is J(_C,A,O,_A).if not J:valid return J. local B is J:x,K is sqrt(max(0,1-U+U*B^2)),Y is sqrt(N*H/2),Z is(C-D)/F,_G is sqrt(max(0,1-Z^2)),_D is A*K-B,_E is A*K+B,_H is Y*(_D-Z*_E)/C,_I is-Y*(_D+Z*_E)/D,_F is Y*_G*(K+A*B),_J is _H*S+_F/C*W,_K is _I*T+_F/D*X. return lex("valid",true,"departureVelocity",_J,"arrivalVelocity",_K,"direction",G,"revolutions",O,"branch",_A,"x",B,"y",K,"lambda",A,"T",_C,"chord",F,"semiperimeter",H,"iterations",J:iterations).}).}